// "Solution" tsconfig for code that runs in Node.js (scripts)
{
  "compilerOptions": {
    "noEmit": true,
    // Needed for `gts clean`, nothing else
    "outDir": "build"
  },
  "exclude": ["src"]
}
